name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  frontend-checks:
    name: Frontend checks
    if: >-
      github.event_name == 'pull_request' ||
      contains(join(github.event.commits.*.modified, ','), 'src/') ||
      contains(join(github.event.commits.*.added, ','), 'src/') ||
      contains(join(github.event.commits.*.removed, ','), 'src/') ||
      contains(join(github.event.commits.*.modified, ','), 'public/') ||
      contains(join(github.event.commits.*.added, ','), 'public/') ||
      contains(join(github.event.commits.*.removed, ','), 'public/') ||
      contains(join(github.event.commits.*.modified, ','), 'index.html') ||
      contains(join(github.event.commits.*.added, ','), 'index.html') ||
      contains(join(github.event.commits.*.removed, ','), 'index.html') ||
      contains(join(github.event.commits.*.modified, ','), 'package.json') ||
      contains(join(github.event.commits.*.added, ','), 'package.json') ||
      contains(join(github.event.commits.*.removed, ','), 'package.json') ||
      contains(join(github.event.commits.*.modified, ','), 'pnpm-lock.yaml') ||
      contains(join(github.event.commits.*.added, ','), 'pnpm-lock.yaml') ||
      contains(join(github.event.commits.*.removed, ','), 'pnpm-lock.yaml') ||
      contains(join(github.event.commits.*.modified, ','), 'vite.config.') ||
      contains(join(github.event.commits.*.added, ','), 'vite.config.') ||
      contains(join(github.event.commits.*.removed, ','), 'vite.config.') ||
      contains(join(github.event.commits.*.modified, ','), 'vitest.config.') ||
      contains(join(github.event.commits.*.added, ','), 'vitest.config.') ||
      contains(join(github.event.commits.*.removed, ','), 'vitest.config.') ||
      contains(join(github.event.commits.*.modified, ','), 'tsconfig') ||
      contains(join(github.event.commits.*.added, ','), 'tsconfig') ||
      contains(join(github.event.commits.*.removed, ','), 'tsconfig') ||
      contains(join(github.event.commits.*.modified, ','), 'tailwind.config.') ||
      contains(join(github.event.commits.*.added, ','), 'tailwind.config.') ||
      contains(join(github.event.commits.*.removed, ','), 'tailwind.config.') ||
      contains(join(github.event.commits.*.modified, ','), '.github/workflows/ci.yml') ||
      contains(join(github.event.commits.*.added, ','), '.github/workflows/ci.yml') ||
      contains(join(github.event.commits.*.removed, ','), '.github/workflows/ci.yml')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build frontend
        run: pnpm build

      - name: Run frontend tests
        run: pnpm test

  rust-checks:
    name: Rust checks
    if: >-
      github.event_name == 'pull_request' ||
      contains(join(github.event.commits.*.modified, ','), 'src-tauri/') ||
      contains(join(github.event.commits.*.added, ','), 'src-tauri/') ||
      contains(join(github.event.commits.*.removed, ','), 'src-tauri/') ||
      contains(join(github.event.commits.*.modified, ','), '.github/workflows/ci.yml') ||
      contains(join(github.event.commits.*.added, ','), '.github/workflows/ci.yml') ||
      contains(join(github.event.commits.*.removed, ','), '.github/workflows/ci.yml')
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy

      - name: Cache Cargo registry and target
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            src-tauri/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('src-tauri/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Install macOS build dependencies
        run: brew install pkg-config

      - name: Build Rust backend
        run: cargo build --manifest-path src-tauri/Cargo.toml

      - name: Run Rust tests
        run: cargo test --manifest-path src-tauri/Cargo.toml

      - name: Run clippy
        run: cargo clippy --manifest-path src-tauri/Cargo.toml

  landing-page:
    name: Landing page
    if: >-
      github.event_name == 'pull_request' ||
      contains(join(github.event.commits.*.modified, ','), 'www/') ||
      contains(join(github.event.commits.*.added, ','), 'www/') ||
      contains(join(github.event.commits.*.removed, ','), 'www/') ||
      contains(join(github.event.commits.*.modified, ','), '.github/workflows/ci.yml') ||
      contains(join(github.event.commits.*.added, ','), '.github/workflows/ci.yml') ||
      contains(join(github.event.commits.*.removed, ','), '.github/workflows/ci.yml')
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: www
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: pnpm
          cache-dependency-path: www/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build landing page
        run: pnpm build
