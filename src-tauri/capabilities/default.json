{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main and recording overlay windows",
  "windows": ["main", "recording-overlay"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
